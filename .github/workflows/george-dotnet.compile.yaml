name: Compile NuGet Packages

on:
  pull_request:
    branches:
      - main
    paths:
      - 'george-dotnet/**/*'
      - '.github/workflows/george-dotnet.compile.yaml'
  workflow_call:

jobs:
  compile:
    name: compile nuget packages
    runs-on: ubuntu-latest
    steps:
      - name: checkout repository
        uses: actions/checkout@v4
    
    